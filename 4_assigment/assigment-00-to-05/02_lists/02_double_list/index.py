def double_numbers(numbers):
    return [num * 2 for num in numbers]  # List comprehension for doubling

# Example usage
numbers = [1, 2, 3, 4]
doubled_numbers = double_numbers(numbers)

print("Doubled numbers:", doubled_numbers)
